#include <stdio.h>
#include <locale.h>

int main(){
    char *locale = setlocale(LC_ALL, "");
    float a = 0;
    float b = 0;
    printf("Введите a: ");
    scanf("%e", &a);
    printf("Введите b: ");
    scanf("%e", &b);
    float s = (a+b)/2;
    printf("Среднее арифметическое = %g \n", s);
    return 0;
}
